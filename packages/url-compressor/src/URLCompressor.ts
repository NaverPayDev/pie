/* eslint-disable @typescript-eslint/no-unused-vars */

export class URLCompressor {
    compress(urlObj: Record<string, string>) {
        return ''
    }

    decompress(compressedUrl: string, paramKeys: string[]) {
        return {}
    }
}
